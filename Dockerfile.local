FROM ubuntu:24.04
RUN apt-get update && apt-get install -y ca-certificates curl && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY toolbox-bin /toolbox
RUN chmod +x /toolbox
EXPOSE 8080
ENTRYPOINT ["/toolbox", "--prebuilt", "looker", "--address", "0.0.0.0", "--port", "8080", "--ui"]
